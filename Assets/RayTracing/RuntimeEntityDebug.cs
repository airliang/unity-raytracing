using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class RuntimeEntityDebug : MonoBehaviour
{
    public List<int> meshInstanceIDs = new List<int>();
    public int lightIndex = -1;
    public int meshInstBVHAddress = -1;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
